# Base URL of the Verdiles admin app.
# Login and the primary CTAs both point here (the admin gates on auth at its root).
# Current production admin host; switch to https://app.verdiles.com once that host exists.
VITE_APP_URL=https://e-commerce-47038.web.app
